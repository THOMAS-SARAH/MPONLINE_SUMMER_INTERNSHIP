//BASIC DATA STRUCTURE
//ARRAYS
//JAGGED ARRAY -  array of arrays
// syntax:
/*int[]={};
int[][]//jagged array*/

/*basic 1D array code*/

using System;
using System.Collections.Generic;
using System.Text;

namespace proj1
{
    internal class ArrayExample
    {
        public static void m1()
        {
            int[] numbers = { 1, 2, 3, 4, 5, };
            Console.WriteLine(numbers[0]);

            for(int i=0;i<numbers.Length; i++)
            {
                Console.WriteLine(numbers[i]);
            }
            //using foreach
            //foreach(datatype id in nameofCollection)
/*
 * for and foreach
 * foreach is readonly -> Console.ReadLine() is not possible
 *foreach always start with 0 index; no specific condition is allowed, cant have end point 
 */
            foreach(int a in number)
            {
                Console.WriteLine(a);
            }
    }

public static void m2
{

}

public static void m
{

}
}
